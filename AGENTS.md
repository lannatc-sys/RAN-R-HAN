# กฎการทำงานสำหรับ AI Agent ในโปรเจกต์นี้

ไฟล์นี้คือกฎเดียวที่ทุก agent (Claude Code, Codex, Antigravity/Gemini) ต้องอ่านก่อนเริ่มงาน
ทุกข้อด้านล่างมาจากความผิดพลาดที่เกิดขึ้นจริงในโปรเจกต์นี้ ไม่ใช่คำแนะนำทั่วไป

---

## 1. ก่อนเริ่มงาน — ยืนยันว่าอยู่ถูกที่

```bash
git rev-parse --abbrev-ref HEAD && git log --oneline -1 && git status --short
```

**ถ้า `git status` สะอาดผิดปกติ หรือหาไฟล์ที่ควรมีไม่เจอ ให้หยุดแล้วตรวจก่อน อย่าสรุปว่าไม่มีงานค้าง**

> **เคยเกิดขึ้นแล้ว:** session หนึ่งถูกสร้างเป็น worktree ที่ commit เก่ากว่าของจริง 5 commit
> `git status` ขึ้นว่าสะอาด มองไม่เห็นงานเลยสักไฟล์ จน build และ test ที่รันไปทั้งหมด
> **ไม่มีน้ำหนักเป็นหลักฐาน** เพราะรันกับ tree ที่ไม่มีโค้ดที่จะตรวจ

worktree ต้องตั้งฐานจาก branch ที่มีงานจริงเสมอ ไม่ใช่ปล่อยให้ default ไปเกาะ commit เก่า

## 2. ห้ามเขียนสิ่งที่ยังไม่ได้ตรวจลงเอกสาร

- รันแล้วผ่าน → เขียนผลจริงพร้อมตัวเลข
- ไม่ได้รัน → เขียน **NOT PERFORMED** ตรง ๆ
- **ห้ามเขียนว่า "ทดสอบบน production สำเร็จ" ถ้าไม่ได้ทดสอบเอง**

> **เคยเกิดขึ้นแล้ว:** เอกสารเคยระบุจำนวนเทสต์ขัดกันเอง 3 ที่ (169 / 171 / 222)
> และเคยมี gate ที่เขียนว่าผ่านทั้งที่ยืนยันไม่ได้ ทำให้คนอ่านเข้าใจผิดว่าพร้อม deploy

## 3. เทสต์ผ่านไม่ได้แปลว่าใช้งานได้

เทสต์ SQL ในโปรเจกต์นี้ **อ่านไฟล์ migration เป็นข้อความแล้ว regex** ไม่ได้รัน SQL จริง

- migration ที่ syntax ผิดก็ยังผ่านเทสต์ได้
- **ก่อนบอกว่า migration ใช้ได้ ต้องรันจริง:** `pnpm test:db:service-area`

> **เคยเกิดขึ้นแล้ว:** migration ไฟล์หนึ่งมี BOM นำหน้า ทำให้ Postgres คืน syntax error
> ตั้งแต่อักขระแรก แต่ **253 เทสต์ผ่านหมดและ tsc ผ่าน** จับไม่ได้เลย

## 4. แก้บั๊กแบบรูปแบบ ต้องกวาดทั้งโปรเจกต์

เจอช่องโหว่หนึ่งจุด ให้ grep หาทุกจุดที่มีรูปแบบเดียวกันก่อนบอกว่าแก้เสร็จ

> **เคยเกิดขึ้นแล้ว 2 ครั้ง:** ครั้งแรกตรวจ IDOR แค่ไฟล์เดียวแล้วรายงานว่าเจอ 2 จุด
> ทั้งที่มีอีก 5 ไฟล์ที่เป็นแบบเดียวกัน ครั้งที่สองแก้ 4 ฟังก์ชันในไฟล์หนึ่ง
> แต่เหลือตัวที่ 5 ในไฟล์เดียวกันเปิดอยู่

## 5. เทสต์ที่เพิ่มต้องพิสูจน์ว่าไม่ใช่เทสต์ลอย

ต้องยืนยันว่า **fail กับโค้ดก่อนแก้** วิธีตรวจ

```bash
git worktree add --detach /tmp/rg <commit-ก่อนแก้>
cp test/<ไฟล์เทสต์> /tmp/rg/test/
cd /tmp/rg && npx tsx --test test/<ไฟล์เทสต์>   # ต้อง fail
git worktree remove --force /tmp/rg
```

## 6. ความปลอดภัย — สแกนก่อน commit เสมอ

repo นี้เป็น **PUBLIC** ของที่ push แล้วถือว่ารั่วถาวร ลบไฟล์ทีหลังไม่ช่วย

```bash
git diff --cached | grep -inE "password|secret|api[_-]?key|token|eyJ[A-Za-z0-9_-]{20,}"
```

ตรวจไฟล์ `.docx`/binary ด้วย เพราะ grep ธรรมดามองไม่เห็น (`unzip -p file.docx | grep ...`)

> **เคยเกิดขึ้นแล้ว:** รหัสผ่านถูก commit ขึ้น public repo ผ่าน PR
> และ `CRON_SECRET` ตัวจริงเกือบถูก commit ตามไปด้วย จับได้ตอนสแกนก่อน commit พอดี

## 7. Server Action คือ endpoint สาธารณะ

ทุก `export async function ...Action` ยิงจากภายนอกได้ **ถ้าใช้ `createAdminClient()` ต้องตรวจสิทธิ์ก่อนเสมอ**

- superadmin → `checkIsSuperadmin()`
- ระดับร้าน → `has_shop_access(shop_id)`
- ตรวจ **ก่อน** แตะ admin client และ return ทันทีเมื่อไม่ผ่าน

## 8. Migration

- **ห้ามแก้ migration ที่ commit หรือ deploy ไปแล้ว** ให้เพิ่มไฟล์ใหม่แล้ว `create or replace` ทับ
- ห้ามมี BOM — สร้างไฟล์ด้วย PowerShell `Set-Content -Encoding UTF8` จะได้ BOM ติดมา ให้ใช้วิธีอื่น
- ลงทะเบียนขั้นใหม่ใน `scripts/run-db.js` ด้วย ไม่งั้นจะไม่ถูกรัน

## 9. บันทึกงานที่ทำ

เมื่อจบงาน ให้อัปเดต **`docs/HANDOFF.md` ไฟล์เดียว** ระบุ

1. ทำอะไรไป และ commit ไหน
2. ผลตรวจจริง (test / build / DB) พร้อมตัวเลข
3. อะไรที่ยังไม่ได้ตรวจ — เขียน NOT PERFORMED
4. งานที่เหลือและตัวบล็อก

**ห้ามสร้างไฟล์ handoff ใหม่แยกออกมา** เคยมี 3 ไฟล์แล้วข้อมูลขัดกันเอง จนต้องรวมกลับเป็นไฟล์เดียว
ถ้าข้อมูลเดิมล้าสมัย ให้แก้ของเดิม ไม่ใช่เขียนไฟล์ใหม่ทับ

## 10. เมื่อ build พังแบบหาสาเหตุไม่เจอ

ล้าง `node_modules` ก่อนเป็นอย่างแรก โดยเฉพาะหลังย้ายเครื่องหรือย้ายดิสก์

```bash
rm -rf node_modules .next && pnpm install --frozen-lockfile
```

> **เคยเกิดขึ้นแล้ว:** build พังไม่คงที่ มีการสรุปผิดไป 2 รอบ
> (โทษ `.next` cache รอบหนึ่ง โทษบั๊ก Node 24 อีกรอบ) สาเหตุจริงคือ `node_modules`
> ที่ติดมาจากการย้าย SSD ข้ามเครื่อง

## 11. ทำงานหลาย agent พร้อมกัน

- **ห้าม 2 agent แก้ไฟล์ใน checkout เดียวกันพร้อมกัน** ตกลงกันก่อนว่าใครถือ
- อีกตัวทำได้เฉพาะงานอ่าน หรือยิง HTTP ที่ไม่แตะไฟล์
- คนแก้กับคนตรวจควรเป็นคนละตัว การตรวจงานตัวเองมีน้ำหนักน้อยกว่า

---

**อ่านต่อ:** [`docs/HANDOFF.md`](./docs/HANDOFF.md) — สถานะปัจจุบัน งานค้าง และหลักฐานคุณภาพล่าสุด
